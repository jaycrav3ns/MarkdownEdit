  const boldStart = "**";
  const boldEnd = "**";
  const bold = `${boldStart}${boldEnd}`;

  const underlineStart = "__";
  const underlineEnd = "__";
  const underline = `${underlineStart}${underlineEnd}`;

  const italicStart = "//";
  const italicEnd = "//";
  const italic = `${italicStart}${italicEnd}`;

  const strikeStart = "--";
  const strikeEnd = "--";
  const strike = `${strikeStart}${strikeEnd}`;

  const sourceStart = "%% ";
  const sourceEnd = " %%";
  const source = `${sourceStart}${sourceEnd}`;

  const centerStart = "%%(wacko wrapper=text wrapper_align=center)";
  const centerEnd = "%%";
  const center = `${centerStart}${centerEnd}`;

  const rightStart = "%%(wacko wrapper=text wrapper_align=right)";
  const rightEnd = "%%";
  const right = `${rightStart}${rightEnd}`;

  const justifyStart = "%%(wacko wrapper=text wrapper_align=justify)";
  const justifyEnd = "%%";
  const justify = `${justifyStart}${justifyEnd}`;

  const mktableStart = "";
  const mktableEnd = "\\n#|\\n|| | ||\\n|| | ||\\n|#\\n";
  const mktable = `${mktableStart}${mktableEnd}`;

  const olStart = "  1. ";
  const olEnd = "  1. ";
  const ol = `${olStart}${olEnd}`;

  const ulStart = "  * ";
  const ulEnd = "";
  const ul = `${ulStart}${ulEnd}`;

  const indentStart = "  ";
  const indentEnd = "  ";
  const indent = `${indentStart}${indentEnd}`;

  const htmlStart = "<# ";
  const htmlEnd = " #>";
  const html = `${htmlStart}${htmlEnd}`;

  const hyperlinkStart = "((";
  const hyperlinkEnd = " ))";
  const hyperlink = `${hyperlinkStart}${hyperlinkEnd}`;
